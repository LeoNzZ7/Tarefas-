import Head from "next/head";

export default function Dashboard() {
    return (
        <div className="dark:bg-[#0f0f0f] h-screen transition-all" >
            <Head>
                <title>Tarefas+ | Painel de tarefas</title>
            </Head>
            ...
        </div>
    )
}